Part B — Capacity reconciliation

Files:
- calculations/kv_capacity.py : computes KV bytes per token and estimates concurrent sequences on the GPU

Run:
  python partB/calculations/kv_capacity.py

The script prints JSON with the arithmetic and results. Ensure Python 3 is available.
